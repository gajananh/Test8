1.6 


			nction log(sMsg) {
        Packages.mks.util.Logger.message(sMsg);
}

// Aborts the script and rolls back changes
function abort(s) {
        eb.abortScript(s, true);
}
nction log(sMsg) {
        Packages.mks.util.Logger.message(sMsg);
}

// Aborts the script and rolls back changes
function abort(s) {
        eb.abortScript(s, true);
}
nction log(sMsg) {
        Packages.mks.util.Logger.message(sMsg);
}

// Aborts the script and rolls back changes
function abort(s) {
        eb.abortScript(s, true);
}
	